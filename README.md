# woodpecker-test
Test repository for Woodpecker CI baseline pipeline injection

## Purpose
This repo tests the config-service's ability to inject baseline security scanning steps into application pipelines.
